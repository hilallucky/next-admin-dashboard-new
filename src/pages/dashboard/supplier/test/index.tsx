import DefaultLayout from '@/ui/Layouts/DefaultLayout';
import FormPage from './FormPage';

const index = () => {
  return (
    <div>
      <DefaultLayout>
        <FormPage />
      </DefaultLayout>
    </div>
  );
};

export default index;
